@page "/logout"
@attribute [Authorize]
@using SecureVault.App.Services.Service.Contracts

@code {
    [Inject] private IAuthService AuthService { get; set; }

    protected override async Task OnInitializedAsync() => await AuthService.LogoutAsync();
}